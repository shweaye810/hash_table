simply download all file then compile with
g++ -std=c++11 -o find_path.out find_path.cpp 

Project is to find a path between two word by changing one letter each time,
until it matches with the other word.
